<ul class="aman-list">
<% for (const item of items) { %>
  <% const href = item['external-url'] || item.path; %>
  <li><a href="<%- href %>"<% if (item['external-url']) { %> target="_blank" rel="noopener"<% } %>><%= item.title %></a></li>
<% } %>
</ul>
